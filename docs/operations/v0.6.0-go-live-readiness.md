# v0.6.0 go-live readiness

## Assessment

**READY for v0.6.0 release preparation.**

The final `develop` candidate has complete accumulated evidence for issue #187. The original full campaign,
the focused restore-verification rerun for #214, and the focused isolated initial-import rerun for #216 are
recorded below with their own candidate and provenance. The real operational database did not receive
synthetic acceptance fixtures.

This is readiness for release preparation, not #188 cutover. The release tag does not yet exist, and the real
initial import remains a conditional #188 step only if the operational catalog is still uninitialized.

## Final candidate and evidence provenance

- Final `develop` candidate: `40fef7fde41f240162dd95d2a2ca792244b70c9a`.
- Original full validation candidate: `d5f2c9631bad7fc9fbac16255e133c7b90f2e37e`.
- #214 focused rerun candidate: `cfab417b81ab0374e01dc4226e0f0c3225695754`.
- #216 focused rerun and final-candidate physical accessibility smoke: `40fef7fde41f240162dd95d2a2ca792244b70c9a`.

The specification was captured after the first partial execution. Its at-capture `NOT READY` state and then
remaining-evidence list are historical; this report is the closeout evidence record.

### Reuse after changed-path assessment

- `d5f2...` to `cfab417...` changed only the restore verifier, its contract test/CI integration, and restore
  documentation. Notebook/tablet UI, security, inventory, production, network, lifecycle, and resource
  evidence from `d5f2...` is **reused evidence** because those paths were unaffected. The restore path was
  rerun physically instead.
- `cfab417...` to `40fef7...` changed only the opt-in initial-import execution boundary, a servlet-only
  security configuration condition, focused tests, and initial-import/runbook documentation. The #216 focused
  validation proved the changed non-web path; normal servlet authentication, CSRF, sessions, and protected
  routes were preserved by the focused implementation validation. Other prior physical scenarios are **reused
  evidence**, not claimed as rerun. The prepared Galaxy Book could access the final candidate after the merge;
  that is a post-change accessibility smoke only.

The #186 workstation record remains historical context only. It is not credited as a fresh current-candidate
UI, workstation, network, lifecycle, or backup result.

## Fresh automated and isolated evidence — `d5f2...`

The original full campaign passed on 2026-09-10:

- `backend/ ./mvnw verify`: **PASS** — 441 tests.
- `frontend/ pnpm lint`: **PASS**.
- `frontend/ pnpm test`: **PASS** — 57 files / 321 tests.
- `frontend/ pnpm build`: **PASS**.
- No-cache local operational image/runtime and fresh PostgreSQL startup: **PASS** — all 14 Flyway migrations,
  health `UP`, private PostgreSQL port, exactly two operational services, persistence across application
  recreation and full Compose restart.
- Isolated security/session/CSRF/XSRF/logout acceptance: **PASS** — protected access rejected when
  unauthenticated, invalid writes rejected, valid authenticated frontend writes accepted, restart invalidated
  the in-memory session, and logout removed effective access.
- Isolated inventory acceptance: **PASS** — exact `NUMERIC(19,6)` values, receipt, backend-authoritative
  FEFO, expiration boundary, signed adjustment, physical loss, expired disposal, refreshed balances/alerts,
  and immutable history.
- Isolated production and traceability acceptance: **PASS** — formula, exact source-batch consumption, output
  batch/lot, and recursive genealogy.
- Isolated backup/checksum/guarded restore with representative data: **PASS** — restored application health,
  Flyway history, and catalog/batch/movement/formula/production/genealogy relationship checks.

All destructive/read-write acceptance in this campaign used generated credentials, disposable Compose
projects/networks/volumes, and temporary files. Those resources were removed afterward.

## Fresh physical evidence — `d5f2...`

### Notebook and tablet

The prepared Galaxy Book and supported tablet passed the following scenarios against the original campaign
candidate:

- Notebook: operational shortcut, operator login, dashboard, primary routes, compact/expanded navigation,
  keyboard focus, branding, contextual guidance, reference metadata, helper text, navigation states, and
  browser close/reopen.
- Tablet: trusted-LAN access, login, dashboard/routes, responsive drawer, touch navigation without a
  hover-only dependency, no relevant clipping, branding/guidance/reference metadata/helper text,
  logout/login, and browser reopen.
- Session invalidation: an authenticated browser session was invalidated by application-container restart;
  navigation to a protected route returned the real Angular UI to login without a manual refresh. This proves
  the required invalid-session UI outcome; it does **not** claim that a literal 12-hour timeout elapsed.
- Physical tablet CSRF-protected write: **PASS** against an isolated disposable database; representative
  supplier creation succeeded and the disposable environment was removed. The real operational database was
  not touched.

### Host lifecycle, topology, resources, and exposure

- Windows reboot without manually stopping/starting containers: **PASS** — application and healthy PostgreSQL
  recovered automatically, health was `UP`, shortcut worked, and tablet access worked.
- Browser closure did not stop the runtime. Plugged-in closed lid remained available; sleep, shutdown, and
  Wi-Fi disconnection made the application unavailable as documented; wake, cold start, and Wi-Fi reconnect
  restored availability. Browser access after cold start required authentication again, consistent with the
  in-memory session design.
- Runtime topology: **PASS** — application on TCP 8080, PostgreSQL only container-private on `5432/tcp`, and
  exactly the two expected operational services.
- Idle observation: **PASS** — application about 604.2 MiB RAM, PostgreSQL about 53.6 MiB RAM, low idle CPU,
  stable memory, and no continuous abnormal idle activity.
- Current network inspection: **PASS** — Windows inbound TCP 8080 allow rule restricted to the Private profile
  and `LocalSubnet`, edge traversal blocked, no enabled inbound TCP 5432 rule, and no router forwarding,
  DMZ, UPnP, or DDNS exposure.
- Current backup evidence: **PASS** — a backup and checksum sidecar were created, verified locally, copied to
  the approved off-notebook Google Drive location, copied back to a temporary verification location, and
  checksum-verified again. No dump contents or private paths are recorded.

## Focused blocker rerun — #214 on `cfab417...`

#214 corrected the generic restore verifier, which had incorrectly rejected a valid sparse operational backup
by demanding representative positive business-row counts. It did not change backend/frontend business
behavior, schema, or Flyway migrations.

The physical Galaxy Book restore rerun passed:

- backup checksum verification and all 14 Flyway migrations;
- zero broken catalog/batch, movement, formula, production-output, production-source, and genealogy
  relationships for rows that existed;
- restored current application startup and final health `UP`;
- disposable restore cleanup; and
- continued health of the operational runtime.

Transient startup-poll connection-reset output was non-blocking because the final restored health check passed.

## Focused blocker rerun — #216 on `40fef7...`

#216 replaced the stale host-JAR procedure with the current operational image invoked through
`docker compose run --rm --no-deps`. The approved frozen external CSV was checksum-verified and mounted
read-only; its rows, credentials, and private location are not recorded.

On the prepared Galaxy Book, a `lavanda-flow-import-validation-*` disposable project passed:

- PostgreSQL 17 startup and all 14 Flyway migrations;
- `DRY_RUN`: 92 rows, 3 catalog-only, 89 opening-stock, 0 rejected, and clean one-off application exit;
- `APPLY` against the same disposable target: 92 rows, 3 catalog-only, 89 opening-stock, 0 rejected, and
  clean one-off application exit;
- cleanup: application container, PostgreSQL container, network, and PostgreSQL volume removed, with no
  disposable import resources remaining; and
- operational runtime after cleanup: application `Up`, PostgreSQL `Up (healthy)`, private PostgreSQL port,
  and health `UP`.

The narrow production change allows the intentional non-web importer to start without servlet `HttpSecurity`
and exit cleanly after success. Normal servlet runtime, authentication, CSRF, sessions, and protected routes
remain unchanged.

## Runbook and blocker status

The current Galaxy Book maintainer path now has no undocumented critical step: status/health/lifecycle,
backup and sparse restore verification, and the conditional isolated import invocation have all been exercised
through their documented paths. #214 and #216 were separate focused issues, not hidden #187 feature work.

| Former blocker | Resolution |
| --- | --- |
| Sparse valid backup rejected by restore verification | Resolved by #214 and the fresh physical restore rerun. |
| Stale host-JAR initial-import command | Resolved by #216 and the fresh isolated Windows `DRY_RUN`/`APPLY` rerun. |
| Angular invalid-session UI behavior | Resolved by the real-client container-restart invalidation scenario; no literal timeout duration is claimed. |
| Physical tablet CSRF-protected write | Resolved on an isolated disposable database. |
| Closed-lid/sleep/shutdown/Wi-Fi lifecycle | Resolved by fresh host-lifecycle observations. |
| Current firewall/router exposure | Resolved by fresh current network inspection. |
| Off-notebook backup round trip | Resolved by fresh checksum-verified copy-back. |

## Known non-blocking warnings

- Angular initial-bundle warning.
- CycloneDX schema-keyword warnings.
- Lombok `Unsafe` deprecation warning.
- Mockito dynamic-agent notice.
- Transient restore startup curl/connection-reset polling before final health success.
- The Galaxy Book `localhost:8080` quirk; supported `127.0.0.1` and trusted-LAN paths work.

## Remaining accepted risks

- Availability depends on the local notebook being powered, awake, and connected; there is no HA/PITR.
- Windows, Docker Desktop, firewall, and network configuration can drift and remain maintainer responsibilities.
- The exact release tag does not yet exist.
- #188 must perform the real initial import only if still required, after the documented checksum, `DRY_RUN`,
  empty-target guard, accepted report, backup, and post-import verification.

## Repository state at closeout update

This report update changes documentation only. No acceptance campaign was rerun for this update.

The final handoff records the outputs of `git diff --check`, `git diff --stat`, `git diff`, and
`git status --short` after this report is updated.

## Validation summary

| Gate | Result |
| --- | --- |
| Final candidate | `40fef7fde41f240162dd95d2a2ca792244b70c9a` |
| Original automated/isolated campaign | PASS on `d5f2...` |
| Original notebook/tablet/lifecycle campaign | PASS on `d5f2...` |
| #214 focused restore rerun | PASS on `cfab417...` |
| #216 focused isolated import rerun | PASS on `40fef7...` |
| Final-candidate Galaxy Book accessibility smoke | PASS; not a complete physical rerun |
| Synthetic data in real operational database | None |
| Final assessment | **READY for v0.6.0 release preparation** |
