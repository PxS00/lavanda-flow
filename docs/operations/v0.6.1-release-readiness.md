# v0.6.1 release readiness

## Assessment

**READY for release PR review.**

The repository-preparation phase for issue #233 is complete. This record covers release metadata,
automated regression checks, isolated PostgreSQL and operational-package validation, migration history,
and documentation consistency. It does not claim that the real operator cutover, corrected operational
import, physical filling smoke test, release PR, tag, or GitHub Release has occurred.

## Candidate and scope

- Branch: `release/v0.6.1`.
- Repository-preparation starting commit: `609e180229d84f3b0f7e44112dcfe3814ba95d1a`.
- Release base: `a62d381a4ba1b225f6d9e7b5552980514cea8c09` (`origin/develop`).
- Fresh validation was executed against the reviewed release working tree derived from that starting commit.
  The committed release-branch head and release PR CI are the authoritative final candidate validation.
- Included completed issues: #229, #231, #235, and #232.
- No #230 or unrelated v0.7.0 feature work was added during release preparation.

## Version alignment

- Backend resolved Maven project version: `0.6.1`.
- Frontend package version: `0.6.1`.
- Release product metadata contains no `-SNAPSHOT` suffix.
- `frontend/pnpm-lock.yaml` has no root-package version field and remained unchanged.
- Dependency versions were not changed.

## Fresh automated validation

The following checks were rerun on 2026-09-14 against the release working tree:

| Check | Result |
| --- | --- |
| `backend/ ./mvnw verify` | PASS — 514 tests, 0 failures, 0 errors, 0 skipped |
| Spring Modulith verification | PASS — `ModularityTest` passed |
| `frontend/ pnpm lint` | PASS |
| `frontend/ pnpm test` | PASS — 58 files / 335 tests |
| `frontend/ pnpm build` | PASS |
| `git diff --check` | PASS |

The backend suite used Testcontainers PostgreSQL 17 instances. It validated all 16 Flyway migrations on
fresh schemas and allowed Hibernate schema validation to complete. No operational database or volume was
used.

## Inherited issue-test coverage rerun in the full suites

The release did not redesign already merged behavior or duplicate its tests. The full-suite rerun exercised
the existing issue-owned evidence:

- #229 catalog tests cover `FINISHED_PRODUCT`, `MILLILITER` and `UNIT` item representations, stable shared
  `essenceReference` rules, persistence/API contracts, and product gender semantics. Angular catalog tests
  cover presentation of those persisted values.
- #231 parser and PostgreSQL integration tests cover the exact corrected eight-column snapshot, bulk perfume
  mapping to `FINISHED_PRODUCT`, explicit positive-stock source lots, stable reference/type/gender metadata,
  repeated product identities with distinct lots, `Retirada`, expiration, exact decimals, deterministic
  rejection reporting, `DRY_RUN`, empty-target protection, atomic `APPLY`, and absence of fabricated
  withdrawal history. Test fixtures use temporary CSV content and disposable PostgreSQL only.
- #232 packaged-filling tests cover mixed persisted units, exact backend scaling, multiple bulk source lots,
  auditable output and genealogy, global monthly packaged sequence allocation and reset, concurrent
  allocation, rollback for failed/ineligible/expired stock, preservation of standard internal lot behavior,
  and no automatic output-expiration derivation. Angular tests cover backend-confirmed requirements and the
  rule that preview neither predicts nor reserves a definitive packaged sequence.

## Flyway and release audit

- The v0.6.0 baseline contains migrations V1 through V14, and none of them differ in this candidate.
- V15 is the expected finished-product/shared-reference migration from #229.
- V16 is the expected packaged-product-filling migration from #232.
- #231 adds no migration.
- No migration was edited, reordered, replaced, or added during release preparation.
- A fresh PostgreSQL 17 schema applied V1 through V16 successfully; Flyway reported V16 successful and the
  application completed schema validation.

## Isolated operational-package validation

Docker was available, so `compose.operational.yaml` was validated with the synthetic project
`lavanda-flow-release-validation-233-609e180`, synthetic credentials, a fresh named volume, and host port
`18083`:

- the multi-stage operational image built successfully with frontend and backend version `0.6.1`;
- `pnpm install --frozen-lockfile`, Angular production build, and Maven package completed in the image build;
- the application and PostgreSQL services started, PostgreSQL exposed only container port `5432`, and
  `/actuator/health` returned `UP`;
- the fresh database reported Flyway V16 as its latest successful migration; and
- guarded cleanup removed the disposable containers, network, PostgreSQL volume, and validation image tag.

No real CSV, credentials, backup, private path, operational database, or operational Docker volume was read
or modified.

## Operational documentation review

The corrected import, backup/restore, local runtime, go-live, and packaged-filling documents were reviewed.
The go-live runbook was aligned with the v0.6.1 tag/cutover, required eight-column `Retirada` semantics, and
the current isolated-validation anchor. No stable documentation was rewritten for style.

## Known non-blocking warnings

- Angular reports an initial bundle of 678.24 kB, 178.24 kB above the configured 500.00 kB warning budget.
- CycloneDX reports its existing schema-keyword warnings.
- The Java build reports existing Lombok `Unsafe` deprecation and Mockito dynamic-agent notices.
- Initial host health polling of the isolated operational container returned transient connection resets
  before the bounded retry succeeded with final status `UP`.

## Release blockers

None found during repository preparation.

## Controlled steps still pending

The following steps are intentionally deferred to the reviewed release and operator-cutover process:

1. review and commit the repository-preparation changes;
2. open and review the release PR from `release/v0.6.1` to `main`;
3. merge the release PR with a regular merge commit;
4. tag the production merge commit as `v0.6.1` and publish GitHub Release `v0.6.1`;
5. create and verify the real pre-upgrade backup and off-notebook copy;
6. upgrade the operator notebook from the immutable tag without deleting the PostgreSQL volume;
7. confirm the no-normal-use precondition and restore the approved known-good pre-import state;
8. prepare the corrected external CSV and SHA-256 outside source control;
9. run corrected operational `DRY_RUN`, require `rejected=0`, and complete operator review;
10. run one corrected operational `APPLY`;
11. verify representative products, lots, balances, expiration, and opening `ENTRY` history;
12. run one representative bulk-to-packaged filling smoke flow and verify its lot and genealogy;
13. create and verify the post-import/post-smoke backup and off-notebook copy; and
14. back-sync the production merge ancestry into `develop` and restore planned development snapshot metadata.
