import { registerLocaleData } from '@angular/common';
import localePt from '@angular/common/locales/pt';

export const APPLICATION_LOCALE = 'pt-BR';

registerLocaleData(localePt, APPLICATION_LOCALE);
