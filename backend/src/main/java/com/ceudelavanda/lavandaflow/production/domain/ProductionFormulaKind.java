package com.ceudelavanda.lavandaflow.production.domain;

/** Identifies the production process semantics owned by one formula definition. */
public enum ProductionFormulaKind {
    STANDARD,
    PACKAGED_FILLING
}
