package com.ceudelavanda.lavandaflow.inventory.application.initialsnapshot;

/** Explicit execution mode for the one-time initial inventory snapshot. */
public enum InitialInventoryImportMode {
    DRY_RUN,
    APPLY
}
